# Super = tecla Windows
set $mod Mod4
font pango:Hasklug Nerd Font 10
exec_always --no-startup-id ~/.config/polybar/launch.sh

# Desactivar i3bar
bar { status_command i3status; mode hide }

# Atajos mínimos
bindsym $mod+Return exec alacritty
bindsym $mod+d exec rofi -show drun
